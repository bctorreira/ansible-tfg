- /var/backups/mysql

# This directory exists but the test returns that it doesn't
# The reason for this is read permissions: 'user' can't check for it
# TODO: how to check this, without changing permissions and wihtout allowing root ssh
#- /var/backups/mysql/sqldump