- /etc/backupninja.conf

# These files exist but the test returns that they don't
# The reason for this is read permissions: 'user' can't check for them
# TODO: how to check this, without changing permissions and wihtout allowing root ssh
#- /etc/backup.d/10_database_dump.mysql
#- /etc/backup.d/20_remote_copy.rdiff
#- /etc/backup.d/30_media_backup.sh