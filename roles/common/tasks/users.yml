- name: add admin users
  user:
    name: "{{ item.name }}"
    comment: "{{ item.comment }}"
    uid: "{{ item.uid }}"
    password: "{{ item.password }}"
    group: sudo
    shell: /bin/bash
    state: present
  loop:
    "{{ admin_users }}"

- name: remove old users
  user:
    name: "{{ item }}"
    state: absent
    remove: yes
  loop:
    "{{ old_users }}"
